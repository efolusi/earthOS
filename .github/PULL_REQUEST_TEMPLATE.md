# Summary

<!-- What does this PR do, and why? -->

## Checklist

- [ ] `pnpm turbo lint typecheck test build` passes locally
- [ ] Tests added/updated for new behavior
- [ ] Changeset added (`pnpm changeset`) if a published package changed
- [ ] Rendering changes include a screenshot or recording
- [ ] No per-frame allocations or React/zustand state in hot paths (see CONTRIBUTING)
- [ ] Plugin changes keep `plugin.ts` free of heavy imports and pass contract tests
